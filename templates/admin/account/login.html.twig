<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Connexion administration FlashBnB</title>

    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <script src="https://kit.fontawesome.com/e971e3c315.js" crossorigin="anonymous"></script>

    <style>
        body{
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
    </style>
</head>
<body>
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">
                    <i class="fas fa-lock mr-1"></i>Connexion à l'administration
                </h4>
            </div>
            <div class="card-body">
                {% if error %}
                    <div class="alert alert-danger">
                        Les informations que vous avez fournies ne sont pas valides !
                    </div>
                {% endif %}
                <form method="post">
                    <div class="form-group">
                        <label for="username">Email</label>
                        <input id="username" type="text" class="form-control" placeholder="Adresse email" name="_username" value="{{ username }}">
                    </div>
                    <div class="form-group">
                        <label for="password">Mot de passe</label>
                        <input id="password" type="password" class="form-control" placeholder="" name="_password">
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-lock-open mr-1"></i>Connexion !
                        </button>
                        <a href="{{ path('home') }}" class="btn btn-link">
                            <i class="fas fa-arrow-circle-left mr-1"></i>Retour au site
                        </a>

                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
