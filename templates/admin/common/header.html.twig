<nav class="navbar navbar-expand-lg navbar-dark bg-gradient-primary">
    <a class="navbar-brand" href="{{ path('admin_dashboard') }}"><img id="logoFlashbnb" src="{{ asset('images/logo/logo_flashbnb_vecto_white.png') }}" alt=""></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03"
            aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_dashboard') }}">
                    <i class="fas fa-chart-line mr-1"></i>Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_ads_index') }}">
                    <i class="fas fa-hotel mr-1"></i>Annonces
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_booking_index') }}">
                    <i class="fas fa-folder mr-1"></i>Réservations
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_comment_index') }}">
                    <i class="fas fa-comments mr-1"></i>Commentaires
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_user_index') }}">
                    <i class="fas fa-user mr-1"></i>Utilisateurs
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_option_index') }}">
                    <i class="fas fa-align-justify mr-1"></i>Options
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_promocode_index') }}">
                    <i class="fas fa-qrcode mr-1"></i>Codes promos
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_newsletter_index') }}">
                    <i class="fas fa-envelope mr-1"></i>Newsletter
                </a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item d-flex align-items-center">
                <a href="{{ path('home') }}" target="_blank" class="nav-link">
                    <i class="fas fa-eye mr-1"></i>Voir le site
                </a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {% if app.user.avatarname %}
                            <img src="{{ vich_uploader_asset(app.user, 'imageFile')}}" class="avatar avatar-mini" alt="Avatar de {{ app.user.fullName }}"> {{ app.user.fullName }}
                        {% elseif not app.user.avatarname and app.user.picture %}
                            <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.fullName }}"> {{ app.user.fullName }}
                        {% else %}
                            <img src="{{ asset('images/user/avatar_default.jpeg') }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.fullName }}"> {{ app.user.fullName }}
                        {% endif %}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>