{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur le meilleur réseau d'appartements {% endblock %}

{% block stylesheets %}
    <style>
        {# here, define your CSS styles as usual #}
        .lead{
            font-size: 1.5rem;
        }

    </style>
{% endblock %}


{% block body %}
    <div class="container mt-4">
        <div class="jumbotron text-center">
            <h1 class="display-3">Bienvenue sur FlashBnB</h1><br>
            <p class="lead">Nous mettons en relation les voyageurs et les propriétaires qui veulent profiter
                d'opportuinités en proposant leur bien à la location.</p>
            <hr class="my-4">
            <p class="lead"><b>Vous êtes voyageur ? Vous chercher un logement pour vos déplacements ? <br>Réserver ou créer votre propre annonce !!</b></p>
            <p class="lead">
                <a class="btn btn-primary btn-lg m-2" href="{{ path('ads_index') }}" role="button">Voir les annonces</a>
                <a class="btn btn-warning btn-lg m-2" href="{{ path('ads_create') }}" role="button">Créer votre annonce</a>
            </p>
        </div>

        <h2 class="h1 mt-4 mb-3">Nos appartements stars</h2>
        <div class="row">
            {% for data in bestAds  %}
                {% include 'common/_ad.html.twig' with {'ad': data.annonce} %}
            {% endfor %}
        </div>


        <h2 class="h1 mt-4 mb-3">Nos propriétaires stars</h2>
        <div class="row">
            {% for data in bestUsers  %}
                {% include 'common/_owner.html.twig' with {'user': data.user, 'data': data } %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
